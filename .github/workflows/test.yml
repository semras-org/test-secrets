name: print-secrets

on: 
  workflow_dispatch:

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          github-token: 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
          script: |
            console.log(await github.rest.actions.listRepoSecrets({
              owner: context.repo.owner,
              repo: context.repo.repo
            }))
          
